#!/bin/bash
# Check that parameter IDs haven't changed (breaks session compatibility)

echo 'ℹ️  Parameter file modified - ensure parameter IDs remain stable for backward compatibility'
echo 'Changing parameter IDs will break existing user sessions and presets'
